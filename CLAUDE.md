# bradfordwagner.ansible-role-pack

## Updating the version

Version is controlled by `pack_version` in `defaults/main.yml`.

Check for the latest release at: https://github.com/buildpacks/pack/releases

```yaml
pack_version: vX.Y.Z
```
